# Supabase project settings (Project Settings -> API).
# The anon key is safe to expose in the browser; data access is protected by RLS.
# If these are empty, the app runs in demo-only mode without login.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
